<%= form_with(url: task_comments_url(task_id: task.id), method: :post ) do |f| %>

<div class="field">
  <%= f.label :content  %>
  <%= f.text_field :content %>
</div>

<div class="actions">
  <%= f.submit "Create" %>
</div>

<% end %>